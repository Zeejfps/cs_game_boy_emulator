<Project Sdk="Microsoft.NET.Sdk.WebAssembly">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RuntimeIdentifier>browser-wasm</RuntimeIdentifier>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <PublishAot>false</PublishAot>
    <RunAOTCompilation>true</RunAOTCompilation>
    <WasmBuildNative>true</WasmBuildNative>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <InvariantGlobalization>true</InvariantGlobalization>
    <AssemblyName>GameBoyEmulator.Wasm</AssemblyName>
    <RootNamespace>GameBoyEmulator.Wasm</RootNamespace>
    <!-- This project targets browser-wasm only; JSExport/JSImport are intentionally browser-specific. -->
    <NoWarn>$(NoWarn);CA1416</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../GameboyEmulator.Core/GameboyEmulator.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Folder Include="package\dist\" />
  </ItemGroup>

</Project>
