{
  "name": "gameboy-emulator-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:wasm": "npm run build --prefix ../GameboyEmulator.Wasm/package",
    "dev": "vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "gameboy-emulator": "file:../GameboyEmulator.Wasm/package"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.6.0",
    "vite": "^5.0.0"
  }
}
